# Example: Discovery Review

用户请求：

```text
帮我看看最近有没有新增的竞品 app，值得加入长期跟踪。
```

预期输出：
- 候选 app 审核表
- 每个候选的来源、理由、风险
- 自动给出 `Track` / `Watch` / `Drop`
- `Track` 项写入 `focus-apps.md`
- `Watch` 项写入 `app-compete.md`
- 不确定点写入报告的审核备注

不该做的事：
- 不要把逐条确认变成必经流程
- 不要把 discovery 伪装成深度报告
